
  # Insight Aggregator Prototype

  This is a code bundle for Insight Aggregator Prototype. The original project is available at https://www.figma.com/design/hue5s0jx2Oe0p6oY8X2yKl/Insight-Aggregator-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  